class Console {

	// write a line without return
	external static void write(String line);
	
	// write a line ends with return
	external static void writeln(String line);
	
	// print multiple dynamic into lines
	external static void print(? args);
	
	external static String getln(String info);
	
	external static void movCurUp();
	
	external static void setTitle(String title);
	
	external static void cls();
	
}