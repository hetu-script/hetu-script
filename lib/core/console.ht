external class console {
	// static function assert(assertion, )
	
	static function log(line: str)
	
	static function readLine(info: str) -> str
	
	static function eraseLine()
	
	static function clear()
}
