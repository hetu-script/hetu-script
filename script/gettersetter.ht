class Types {
  var _name = 'the machine'

  construct (name: String) {
    _name = name;
  }

  get name: String {
    return _name
  }

  set name(value: String) {
    _name = value
  }
}

// 程序入口
fun main {
  var t = Types('finch')

  print(3.toString())

  print(t.name)
  
}
