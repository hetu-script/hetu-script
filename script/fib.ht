fun fib(x: num): num {
  
  if (x < 2) return x
  else return fib(x - 2) + fib(x - 1)

}
  
fun main {

  var before = System.now()
	System.print(fib(15))
  var after = System.now()
  System.print(after - before)
  
  
}
