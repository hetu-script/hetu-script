fun getBeauty(x: num, cuteness: num, sexy: num): num {
  var a = min(cuteness, sexy)
  var b = max(cuteness, sexy)
  if (x < ((a + b) / 2)) {
    return (-(x - a) * (x - a)) / 50 + 100
  } else {
    return (-(x - b) * (x - b)) / 50 + 100
  }
}
