import 'hetu:core';
import 'hetu:console';
import 'calculator.ht';

// 程序入口 
void main(){
  // 带有初始化语句的变量定义
  // 从类的构造函数获得对象的实例
  var cal = calculator(6, 7);
  
  // 调用外部函数，调用外部成员函数，字符串类型检查
  Console.print('the meaning of life, universe and everything:\n' + cal.meaning().toString());
}
