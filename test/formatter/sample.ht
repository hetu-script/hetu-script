import 'utility.ht' as util show isEqual

external abstract class Animal {
  external static var Animal.race: str
  external construct(race: str) {
    this.race = race
  }
}

class Converser {
  fun talk(other: Converser)  {}
}

class Profile {
  var name: str
}

class Person extends Animal {
  var name: str
  construct(name: str) : super(name) {
    this.name = name
  }
  fun greeting() {
    print('Hi! I\'m', name)
  }
}

fun main() {
  fun nested([arg: str = 'catchya') -> void {
    const cc = arg
    return cc
  }
  if (nested() == -1) {
    main()
  } else {
    return 1 + 1
  }
  (fun(num) {
    return num * num
  })()
  for (var i = 0; i < max; i *= 5) {
    expect(value: true)
  }
  while {
    i++
    break
    do{
      continue
    } while (value is! bool)
  }
  when (true) {
    1 -> {
      '1'
    }
    2 -> {
      return [1, 2, 3]
    }
    else -> {
      print({
        'name': 'Kevin', 
        'age': 16
      }, 7 > 2 ? true : cal(42))
    }
  }
  var ht = Person('Hetu')
  ht.greeting()
}

